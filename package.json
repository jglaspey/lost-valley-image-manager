{
  "name": "lost-valley-railway-deploy",
  "version": "1.0.0",
  "description": "Railway deployment wrapper for Lost Valley Image Manager",
  "scripts": {
    "build": "cd web-app && npm install && cd client && npm install && cd .. && npm run build",
    "start": "cd web-app && npm start",
    "install": "cd web-app && npm install && cd client && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}